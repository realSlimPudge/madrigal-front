import { ChatForm } from "@/widgets/ChatForm";

export default function CreatePage() {
    return (
        <section className="flex min-h-screen flex-col items-center justify-center gap-8 px-4 py-8">
            <ChatForm />
        </section>
    );
}
